# OS / editor
.DS_Store
Thumbs.db
.vscode/
*.swp

# Local preview / runtime
*.log

# Node (not used by the app, but just in case)
node_modules/

# Internal source decks (do not publish)
*.pptx

# Local-only: parallel modern-SharePoint (SPFx) migration track — not part of the
# classic app; kept locally, never pushed.
modern-track/
